---
"@redbamboo/utility": minor
---

Add shared WebSocket event system: `createWebSocket` factory with auto-reconnect, `WsEventProvider`/`useWsSubscribe`/`useWsSubscribeByType` React primitives, and refactored `useLogStream` to use the shared transport
